continue; // el intérprete vuelve al principio del loop y salta sólo una iteración
break; // te saca del loop
return; // solamente puede aparecer dentro de los cuerpos de las funciones
throw; // lanza excepciones
